'use client';

import '@/app/i18n';

// This component initializes i18n on the client side
// It doesn't render anything, just ensures i18n is initialized
export default function I18nProvider() {
    return null;
}
